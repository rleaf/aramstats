const arr = [6]
const a = {a: 1, b: 2, c: 3}
const b = {a: 1, b: 2, d: 3}
let c
let d = 5
for (const i of arr) {
   i
} 

delete arr[3]
arr
const af = o => (o ** 2)

class Config {
   version = 'v2'
   prefix = `/api/` + this.version
}

const test = new Config();
console.log(test.prefix)